
rules_version = '2';

// Redeploy triggered to ensure 'allow read, write: if true' is active across all collections.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
